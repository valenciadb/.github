name: CI

on:
  push:
    branches:
      - $default-branch
      
  pull_request:
    branches:
      - $default-branch

jobs:
  runTests:
    uses: valenciadb/.github/.github/workflows/nodejs-ci.yaml@main